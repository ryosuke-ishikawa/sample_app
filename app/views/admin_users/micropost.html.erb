<%= provide(:title, "micropost一覧") %>
<div class="row">
    
  <div class="col-md-8">
    <h3>Microposts (<%= Micropost.count %>)</h3>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts, renderer: BootstrapPagination::Rails %>
  </div>
</div>